# Auto-generated by ContainerHive
# Do not edit - regenerate with: ch --project github template ci --provider github --output github/.github/workflows/main.yml --image-name ghcr.io/timo-reymann/containerhive

name: Build Images

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  packages: write

env:
  SNAPSHOT_ID: {{ github.event.pull_request.number }}

jobs:
  # === GENERATE ===
  generate:
    runs-on: ubuntu-latest
    container: ghcr.io/timo-reymann/containerhive:6b62d9a
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: ch -p github generate --build-id "$SNAPSHOT_ID"
      - uses: actions/upload-artifact@v4
        with:
          name: generated
          path: github/dist/
          retention-days: 7

  # === BASE-CI ===

  build-base-ci-amd64:
    runs-on: ubuntu-latest
    container: ghcr.io/timo-reymann/containerhive:6b62d9a
    services:
      dind:
        image: docker:dind
        options: --privileged
        env:
          DOCKER_TLS_CERTDIR: ""
    env:
      DOCKER_HOST: tcp://dind:2375
    needs:
      - generate
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: ch -p github wait --docker-socket
      - run: ch -p github build base-ci --platform amd64 --build-id "$SNAPSHOT_ID"
      - run: ch -p github sbom base-ci --platform amd64 --build-id "$SNAPSHOT_ID"

  build-base-ci-arm64:
    runs-on: ubuntu-latest
    container: ghcr.io/timo-reymann/containerhive:6b62d9a
    services:
      dind:
        image: docker:dind
        options: --privileged
        env:
          DOCKER_TLS_CERTDIR: ""
    env:
      DOCKER_HOST: tcp://dind:2375
    needs:
      - generate
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: ch -p github wait --docker-socket
      - run: ch -p github build base-ci --platform arm64 --build-id "$SNAPSHOT_ID"
      - run: ch -p github sbom base-ci --platform arm64 --build-id "$SNAPSHOT_ID"


  manifest-base-ci:
    runs-on: ubuntu-latest
    container: ghcr.io/timo-reymann/containerhive:6b62d9a
    needs:
      - build-base-ci-amd64
      - build-base-ci-arm64
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: ch -p github finalize base-ci --build-id "$SNAPSHOT_ID"

  test-base-ci:
    runs-on: ubuntu-latest
    container: ghcr.io/timo-reymann/containerhive:6b62d9a
    services:
      dind:
        image: docker:dind
        options: --privileged
        env:
          DOCKER_TLS_CERTDIR: ""
    env:
      DOCKER_HOST: tcp://dind:2375
    needs:
      - manifest-base-ci
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: ch -p github wait --docker-socket
      - run: ch -p github test base-ci --build-id "$SNAPSHOT_ID" --pull
  # === BASE-DEPLOYMENT ===

  build-base-deployment-amd64:
    runs-on: ubuntu-latest
    container: ghcr.io/timo-reymann/containerhive:6b62d9a
    services:
      dind:
        image: docker:dind
        options: --privileged
        env:
          DOCKER_TLS_CERTDIR: ""
    env:
      DOCKER_HOST: tcp://dind:2375
    needs:
      - generate
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: ch -p github wait --docker-socket
      - run: ch -p github build base-deployment --platform amd64 --build-id "$SNAPSHOT_ID"
      - run: ch -p github sbom base-deployment --platform amd64 --build-id "$SNAPSHOT_ID"

  build-base-deployment-arm64:
    runs-on: ubuntu-latest
    container: ghcr.io/timo-reymann/containerhive:6b62d9a
    services:
      dind:
        image: docker:dind
        options: --privileged
        env:
          DOCKER_TLS_CERTDIR: ""
    env:
      DOCKER_HOST: tcp://dind:2375
    needs:
      - generate
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: ch -p github wait --docker-socket
      - run: ch -p github build base-deployment --platform arm64 --build-id "$SNAPSHOT_ID"
      - run: ch -p github sbom base-deployment --platform arm64 --build-id "$SNAPSHOT_ID"


  manifest-base-deployment:
    runs-on: ubuntu-latest
    container: ghcr.io/timo-reymann/containerhive:6b62d9a
    needs:
      - build-base-deployment-amd64
      - build-base-deployment-arm64
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: ch -p github finalize base-deployment --build-id "$SNAPSHOT_ID"

  test-base-deployment:
    runs-on: ubuntu-latest
    container: ghcr.io/timo-reymann/containerhive:6b62d9a
    services:
      dind:
        image: docker:dind
        options: --privileged
        env:
          DOCKER_TLS_CERTDIR: ""
    env:
      DOCKER_HOST: tcp://dind:2375
    needs:
      - manifest-base-deployment
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: ch -p github wait --docker-socket
      - run: ch -p github test base-deployment --build-id "$SNAPSHOT_ID" --pull
