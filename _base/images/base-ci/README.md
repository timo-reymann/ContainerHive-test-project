# Base CI Image

Base CI image with essential build tools and common utilities for continuous integration pipelines.

## Features

- Essential build tools
- Common CI utilities
- Minimal footprint

## Usage

```dockerfile
FROM __hive__/base-ci:v1

# Run your CI tasks
RUN make build
```
