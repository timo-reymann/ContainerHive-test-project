# Auto-generated by ContainerHive
# Do not edit - regenerate with: ch --project github template ci --provider github --output .github/workflows/main.yml --image-name ghcr.io/timo-reymann/containerhive

name: Build Images

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  packages: write

env:
  SNAPSHOT_ID: "${{ github.event.pull_request.number }}"
  CH_IMAGE: ghcr.io/timo-reymann/containerhive:72ee6b9

jobs:
  # === GENERATE ===
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          lfs: true
      - run: mkdir -p "$RUNNER_TEMP/ch"
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -i -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github generate --build-id "$SNAPSHOT_ID"
      - uses: actions/upload-artifact@v4
        with:
          name: generated
          path: github/dist/
          retention-days: 7

  # === BASE-CI ===

  build-base-ci-amd64:
    runs-on: ubuntu-latest
    needs:
      - generate
    steps:
      - uses: actions/checkout@v6
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: mkdir -p "$RUNNER_TEMP/ch"
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -i -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github wait --docker-socket
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github build base-ci --platform amd64 --build-id "$SNAPSHOT_ID"
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github sbom base-ci --platform amd64 --build-id "$SNAPSHOT_ID"

  build-base-ci-arm64:
    runs-on: ubuntu-latest
    needs:
      - generate
    steps:
      - uses: actions/checkout@v6
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: mkdir -p "$RUNNER_TEMP/ch"
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -i -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github wait --docker-socket
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github build base-ci --platform arm64 --build-id "$SNAPSHOT_ID"
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github sbom base-ci --platform arm64 --build-id "$SNAPSHOT_ID"


  manifest-base-ci:
    runs-on: ubuntu-latest
    needs:
      - build-base-ci-amd64
      - build-base-ci-arm64
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: mkdir -p "$RUNNER_TEMP/ch"
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -i -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github finalize base-ci --build-id "$SNAPSHOT_ID"

  test-base-ci:
    runs-on: ubuntu-latest
    needs:
      - manifest-base-ci
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: mkdir -p "$RUNNER_TEMP/ch"
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -i -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github wait --docker-socket
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github test base-ci --build-id "$SNAPSHOT_ID" --pull
  # === BASE-DEPLOYMENT ===

  build-base-deployment-amd64:
    runs-on: ubuntu-latest
    needs:
      - generate
    steps:
      - uses: actions/checkout@v6
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: mkdir -p "$RUNNER_TEMP/ch"
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -i -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github wait --docker-socket
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github build base-deployment --platform amd64 --build-id "$SNAPSHOT_ID"
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github sbom base-deployment --platform amd64 --build-id "$SNAPSHOT_ID"

  build-base-deployment-arm64:
    runs-on: ubuntu-latest
    needs:
      - generate
    steps:
      - uses: actions/checkout@v6
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: mkdir -p "$RUNNER_TEMP/ch"
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -i -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github wait --docker-socket
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github build base-deployment --platform arm64 --build-id "$SNAPSHOT_ID"
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github sbom base-deployment --platform arm64 --build-id "$SNAPSHOT_ID"


  manifest-base-deployment:
    runs-on: ubuntu-latest
    needs:
      - build-base-deployment-amd64
      - build-base-deployment-arm64
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: mkdir -p "$RUNNER_TEMP/ch"
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -i -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github finalize base-deployment --build-id "$SNAPSHOT_ID"

  test-base-deployment:
    runs-on: ubuntu-latest
    needs:
      - manifest-base-deployment
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/download-artifact@v4
        with:
          name: generated
          path: github/dist/
      - run: mkdir -p "$RUNNER_TEMP/ch"
      - run: echo "${{ secrets.GITHUB_TOKEN }}" | docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -i -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" $CH_IMAGE ch -p github login -u "${{ github.actor }}" --password-stdin ghcr.io
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github wait --docker-socket
      - run: docker run --rm --entrypoint="" -u "$(id -u):$(id -g)" -e HOME="$RUNNER_TEMP/ch" -v "$RUNNER_TEMP/ch:$RUNNER_TEMP/ch" -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" -w "$GITHUB_WORKSPACE" -v /var/run/docker.sock:/var/run/docker.sock $CH_IMAGE ch -p github test base-deployment --build-id "$SNAPSHOT_ID" --pull